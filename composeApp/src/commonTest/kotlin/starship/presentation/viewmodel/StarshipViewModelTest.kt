package starship.presentation.viewmodel

import kotlin.test.Test

class StarshipViewModelTest {

    // Omitted: Testing the viewModel since there is currently no native solution for mocking and
    // verifying non interface classes in compose multiplatform

    @Test
    fun `given GetStarShipsUseCase is successful getStarships should update the state correctly`() {
    }

    @Test
    fun `given GetStarShipsUseCase is not successful getStarships should update the state correctly`() {
    }


}
